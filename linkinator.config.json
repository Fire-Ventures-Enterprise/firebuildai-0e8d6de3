{
  "name": "linkinator-config",
  "scripts": {
    "link-check": "linkinator https://app.firebuildai.com --recurse --skip '.*\\.(jpg|jpeg|gif|png|svg|ico|webp|woff|woff2|ttf|eot|pdf)$' --skip 'mailto:' --skip 'tel:' --skip 'javascript:' --timeout 30000",
    "link-check:marketing": "linkinator https://firebuildai.com --recurse --skip '.*\\.(jpg|jpeg|gif|png|svg|ico|webp|woff|woff2|ttf|eot|pdf)$' --skip 'mailto:' --skip 'tel:' --skip 'javascript:' --skip '/app/' --timeout 30000",
    "link-check:portal": "linkinator https://app.firebuildai.com/portal/estimate/test-token --skip '.*\\.(jpg|jpeg|gif|png|svg|ico|webp|woff|woff2|ttf|eot|pdf)$' --timeout 30000"
  },
  "targets": [
    {
      "url": "https://firebuildai.com",
      "name": "Marketing Site",
      "recurse": true,
      "excludePatterns": ["/app/", "/admin/", "/secure-"]
    },
    {
      "url": "https://app.firebuildai.com/app/dashboard",
      "name": "App Dashboard",
      "recurse": false,
      "auth": "required"
    },
    {
      "url": "https://app.firebuildai.com/portal/estimate/test-token",
      "name": "Estimate Portal",
      "recurse": false
    },
    {
      "url": "https://app.firebuildai.com/portal/invoice/test-token",
      "name": "Invoice Portal",
      "recurse": false
    }
  ]
}